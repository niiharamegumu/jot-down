# Preserve layout during Markdown live editing

Jot Down will not switch focused blocks into a smaller plain Markdown presentation because cursor placement must not make headings, task rows, or list items jump. Markdown remains the saved source of truth, but any Markdown notation exposed during editing must preserve the block's visual scale and layout.
